class MovieItemEntity {
  MovieItemEntity({
    required this.id,
    required this.poster,
    required this.title,
    required this.description,
  });
  int id;
  String poster;
  String title;
  String description;
}
